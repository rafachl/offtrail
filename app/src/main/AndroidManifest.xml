<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.unoesc.webmob.offtrail">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone_oftrail"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icone_oftrail"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity_">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.LoginActivity_" />
        <activity
            android:name=".ui.PrincipalActivity"
            android:label="@string/title_activity_principal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.CadastroActivity"></activity>
    </application>

</manifest>